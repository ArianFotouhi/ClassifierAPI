name: Deploy Images to GHCR

env:
  DOTNET_VERSION: '6.0.x'
  
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
      push-store-image:
        runs-on: ubuntu-latest
        defaults:
          run:
            working-directory: './Store'
        steps:
          - name: 'Checkout GitHub Action'
            uses: actions/checkout@main
            
          - name: 'Login to GitHub Container Registry'
            uses: docker/login-action@v1
            with:
              registry: ghcr.io
              username: ArianFotouhi
              password: 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
              
          - name: 'Build Inventory Image'
            run: |
              docker build . --tag ghcr.io/arianfotouhi/store:latest
              docker push ghcr.io/arianfotouhi/store:latest 
